#!/bin/execlineb -P
sudo
foreground { 
    if { ./mountroot }
    if { mount -o bind /dev rootfs/dev }
    if { mount -o bind /proc rootfs/proc }
    if { mount -o bind /sys rootfs/sys }
    emptyenv
    export PATH /bin:/usr/bin:/sbin:/usr/sbin
    chroot rootfs /bin/ash
}
./unmountroot
